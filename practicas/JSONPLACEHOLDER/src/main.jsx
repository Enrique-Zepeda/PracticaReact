import React from "react";
import ReactDOM from "react-dom/client";
import { JsonApi } from "./JsonApi";
import { UsingUseState } from "./UseState";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <UsingUseState />
  </React.StrictMode>
);
